# M1 开关按钮与模拟状态测试记录

状态：待人工验收
验证日期：2026-07-22

## 自动验证

- `corepack yarn lint:fix`：通过。
- `corepack yarn typecheck`：通过。
- `corepack yarn test:ci`：通过，6 个测试文件、30 项测试全部成功。
- `corepack yarn build`：通过，Web、Electron 主进程和 preload 均成功构建。

## 界面验证

- 通道数量由人工输入，范围限制为 1～32；实测输入 5 路可正确生成。
- 通道名称可直接编辑；实测修改为“主照明电源”后，切换通道数量及刷新页面仍保留。
- 32 路在组件内部滚动，常规宽度下行高 44px，无行重叠。
- 标题、状态、图标和操作按钮已按人工检查意见缩小。
- 模拟状态覆盖开启、关闭、未知、离线和执行中。
- 点击后先显示执行中，模拟写入并读回后才更新确认状态。
- 开启按钮确认色为 `#2e7d32`，关闭按钮确认色为 `#c62828`。
- 1920x1080 与 1366x768 布局检查通过。

截图：

- [1920x1080](M1-电源控制-1920x1080.png)
- [1366x768](M1-电源控制-1366x768.png)

## 已知限制

- M1 仅验证模拟寄存器，不发送 TCP/Modbus 报文。
- v1.18.2 生产预览生成的 `dist/index.html` 存在上游内联导入语句异常；本次界面使用开发服务验证，该问题不阻塞 M1。
