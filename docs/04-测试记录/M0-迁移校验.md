# M0 校验

- 仓库：`v1.18.2` / `b587d8344dad7784e9c21b516a52a7e89dc62641`
- 分支：`feature/power-control`
- 远程：`https://github.com/bluerobotics/cockpit.git`
- 子模块：3 个，均处于锁定提交。
- 环境：Node.js `v24.16.0`，Yarn `1.22.22`。
- `yarn typecheck`：通过。
- `yarn build`：通过。
- `yarn lint:fix`：通过。
- 本地预览：`http://127.0.0.1:5050/` 返回 HTTP 200。
- 清理：历史原型、工程内原始会话副本、逐条对话原文、旧 `outputs` 和空 `work` 均已删除。

备注：官方 Windows 安装脚本依赖未提供的 `unzip`；本次用系统 `tar.exe` 解压相同官方 FFmpeg/go2rtc 资源。M0 待人工验收，M1 未开始。
