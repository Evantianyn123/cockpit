# L1 汉化基础与核心界面测试记录

日期：2026-07-31

## 自动验证

- `yarn lint:fix` 与 `yarn lint` 通过，无警告或错误。
- `yarn typecheck` 通过；现有 `languageId not found` 提示未影响退出状态。
- `yarn test:ci` 通过，10 个测试文件、44 项测试全部通过。
- `yarn build` 通过，包含 Web、Electron 主进程和 preload 构建。
- 新增测试覆盖默认中文、非法语言回退、语言持久化、翻译键结构和插值参数一致性。

## 人工验收项目

1. 首次启动默认显示简体中文。
2. 在“界面设置”顶部切换中文和英文，确认主菜单、界面设置与电源控制即时切换。
3. 重启后确认语言保持。
4. 验证电源控制的连接配置、连接测试、通道开关和状态读回未回归。
5. 在 `1920×1080`、`1366×768` 下检查文本、按钮和弹窗布局。

状态：已于 2026-07-31 人工验收通过。可以开始 L2；M4 仍不得启动。
