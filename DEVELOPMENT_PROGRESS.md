# Cockpit 开发进度

## 约束

- 根目录：`D:\03.Work\01.软件开发\06.Cockpit开发`
- 基线：Cockpit `v1.18.2`
- 分支：`feature/power-control`
- 顺序：M3 后先完成 L1/L2 汉化，再进入 M4 实机联调和 M5 稳定交付。
- 门禁：每个里程碑必须经用户人工确认后才能开始下一步。
- 复用原则：以 M3/M4 正式功能为准；已验收内容仅在可直接复用时保留，不能复用的内容须先完成可行性核查并记录后删除。

## 状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 迁移、建库、基线验证 | 已完成（2026-07-21 人工确认） |
| M1 | 开关按钮与模拟状态 | 已完成（2026-07-30 人工确认） |
| M2 | 单通道 RTU-over-TCP | 已完成（2026-07-30 人工确认） |
| M2.5 | TCP Server 可配置收发验证 | 已完成（2026-07-30 人工确认） |
| M3 | 多通道与设置页 | 已完成（2026-07-31 人工确认） |
| L1 | 汉化基础与核心界面 | 已完成（2026-07-31 人工验收） |
| L2 | Cockpit 全界面汉化 | 进行中（2026-07-31） |
| M3.5-A | 电源控制折叠与精简界面 | 未开始（等待 L2 人工验收） |
| M3.5-B | 三维机器人姿态 Widget | 未开始（等待 M3.5-A 人工验收） |
| M4 | 真实设备联调 | 未开始 |
| M5 | 电源控制稳定交付 | 未开始 |

## M0 结果

- [x] 官方 `v1.18.2` 源码、分支、远程和 3 个子模块已验证。
- [x] 依赖安装、类型检查、构建、lint 和本地预览通过。
- [x] 旧工作区 `outputs` 与空 `work` 已删除，旧工作区根目录保留。
- [x] 按用户要求删除历史原型、工程内原始会话副本和逐条对话原文。
- [x] 仅保留简要需求上下文和精简测试记录。

阻塞：M0 无阻塞。M4 前需提供真实寄存器表或已验证报文。

人工验收：用户已于 2026-07-21 确认 M0 通过，并授权开始 M1。

## M1 验收结果

验收调整：根据 2026-07-22 人工检查意见，缩小 Widget 文字、图标、按钮和行高；取消 1/8/32 预设，改为人工输入 1～32 路；通道名称改为可编辑并持久化。调整完成后重新验证最小、常用和最大通道数布局。

- [x] 新增可拖拽的“电源控制”Widget。
- [x] 使用模拟读回实现开启、关闭、未知、离线和执行中状态。
- [x] 确认状态以模拟寄存器读回为准，点击时不直接改变。
- [x] 支持人工输入 1～32 路，并验证任意数量、最大数量和内部滚动。
- [x] 支持逐通道名称编辑，并验证切换数量及刷新后的持久化。
- [x] 缩小文字、图标、按钮与行高，常规宽度下每行 44px。
- [x] 完成类型检查、lint、构建和界面截图。

验证：lint、类型检查、30 项自动测试和生产构建全部通过；人工输入 5 路、32 路滚动、名称持久化、1920x1080 与 1366x768 界面检查通过。

限制：M1 仅使用模拟寄存器，不包含 TCP 或 Modbus 通信。

人工验收：用户于 2026-07-30 确认按计划继续开发，视为 M1 通过并授权开始 M2。

## M2 验收结果

- [x] 在 Electron 主进程实现独立的 RTU-over-TCP Modbus 服务和类型化 IPC。
- [x] 自动测试验证 FC6 写入后 FC3 读回状态闭环。
- [x] 在 Lite 版隐藏电源控制 Widget，保存的布局仍被保留。
- [x] 协议测试覆盖 CRC16、FC3/4、FC5/6/16、异常帧、拆包、合帧、串行队列、超时和重连。
- [x] `yarn lint:fix`、类型检查、38 项自动测试和 Electron 生产构建通过。
- [x] 手工验证 FC6 开关、FC3 状态读回、颜色切换和断线状态，并完成截图及人工验收。

M2 范围：实现单通道 RTU-over-TCP 客户端；不连接 `192.168.2.1:502` 或真实电源设备。M3 再实现多通道配置，M4 才进入实机联调。

环境修复：2026-07-30 已从 Electron `v29.4.6` 官方发行包恢复 `node_modules/electron/dist`，并以 SHA-256 `76793445ba1ab72fa94b8f1e1fc6c123898c5763977d385fdb9dc5d2189b9d02` 校验。`yarn dev:electron` 已成功启动 Electron 主进程和渲染进程。

人工验收：用户于 2026-07-30 确认 M2 通过。M3 保持未开始，等待用户单独授权。

## M2.5 开发记录

- [x] 新增全局、持久化的 TCP Server 地址、端口、从站地址和超时配置，键名为 `cockpit-power-control-connection-v1`。
- [x] 在电源控制 Widget 增加仅桌面版可见的连接测试弹窗，支持 HEX 原始数据双向收发。
- [x] 新增独立诊断 socket、类型化 IPC 和本地临时 TCP Server 自动测试。
- [x] 自动验证地址校验、非法 HEX、连接失败、服务端断开，以及已连接时保存并应用新目标会先断开旧 socket。
- [x] `yarn lint:fix`、类型检查、46 项自动测试和 Electron 生产构建通过。
- [x] 使用 SSCOM 手工验证地址保存与重启恢复、发送、接收、主动断开和重连。

范围：M2.5 仅连接用户启动的 SSCOM TCP Server 或自动化测试临时服务器；不连接真实 485 转网络模块或真实电源设备。M2.5 通过人工验收后，M3 才能开始。

复用核查：M2 的 CRC16、RTU-over-TCP 请求队列和超时/重连处理可直接复用至 M3，予以保留。M2 的固定测试目标不作为正式连接配置；M3 使用 `cockpit-power-control-connection-v1`。

自动验证记录：2026-07-30，原始 TCP 诊断测试使用临时 `127.0.0.1` TCP Server，确认 Cockpit 发送字节完整到达、服务端字节以 TCP 数据块显示、不可用地址不自动重连、服务端关闭后客户端离线，以及应用新端点时旧 socket 先断开。并发执行完整测试与生产构建时，既有 widgets 装载测试触发自身 15 秒超时；改为串行重跑后 46 项测试全部通过。

人工验收：用户于 2026-07-30 确认 M2.5 通过。M3 保持未开始，等待用户启动指令。

## M3 开发记录

- [x] 基于 `cockpit-power-control-connection-v1` 实现正式多通道连接配置，不再将 `127.0.0.1:1502` 作为正式目标。
- [x] 支持手工输入 1～32 路通道配置、配置导入导出、连接测试和只读测试。
- [x] 合并相邻状态寄存器读取，并保持写入后读回确认的状态语义。
- [x] 完成 lint、类型检查、46 项自动测试、Electron 生产构建和本地桌面启动检查。

自动验证：2026-07-30，新增配置测试覆盖八路禁用模板、FC5 数值限制、相邻寄存器合并、状态掩码和版本化导入。`yarn lint:fix`、`yarn typecheck`、`yarn test:ci`（46 项）和 `yarn build` 均通过。

复用与清理：保留 M2 的 RTU-over-TCP 客户端、串行队列和超时/重连；正式服务已改由 `cockpit-power-control-connection-v1` 重建会话。已删除 M1 浏览器端 `SimulatedPowerGateway` 及其测试，单通道固定目标不再存在于正式 Widget 或正式服务中。2026-07-31 已按最终功能范围移除开发模式本地模拟服务器及其专用测试。

人工验收：用户于 2026-07-31 确认 M3 通过。M4 保持未开始，等待用户单独授权；M4 前仍须提供真实寄存器表或已验证报文。

## L1 开发记录

计划调整：用户于 2026-07-31 要求将汉化前置。新增 L1“汉化基础与核心界面”和 L2“Cockpit 全界面汉化”，替代未启动的 M6/M7；M4 仍表示真实设备联调，M5 仍表示电源控制稳定交付。L1、L2 均须单独人工验收，L2 未通过前不得进入 M4。

已完成自动验证：接入双语语言服务，默认简体中文；实现本机语言持久化、界面设置选择器、Vue I18n/Vuetify 同步，并覆盖主菜单、界面设置和电源控制核心界面。新增翻译键结构、插值参数和语言持久化测试。2026-07-31 已通过 `yarn lint:fix`、`yarn lint`、`yarn typecheck`、`yarn test:ci`（44 项）和 `yarn build`。L1 不修改 Modbus 协议、寄存器配置或状态读回逻辑。

人工验收：用户于 2026-07-31 确认 L1 通过，并授权开始 L2。M4 仍未开始。

## L2 开发记录

进行中：全面覆盖主界面、设置、任务规划、视频、摇杆、遥测、告警、Actions、DataLake、DIY 编辑器、开发工具、教程、关于、Widget 和用户可见错误提示。保留 Cockpit、BlueOS、MAVLink、协议字段、飞控模式、日志原文和用户输入内容。L2 未经人工验收不得启动 M4。

自动验证：2026-08-16，新增运行时旧界面文本翻译、简体中文/英文切换、Vuetify 同步及桌面语言镜像；“载具发现”启动弹窗及加载页已接入翻译资源。用户自定义的视图名、Widget 名、通道名和 DIY HTML 已排除在通用 DOM 翻译之外。`yarn lint:fix`、`yarn typecheck`、`yarn test:ci`（46 项）和 `yarn build` 均通过。

人工验收待办：检查中文和英文即时切换、重启后语言保持、用户自定义名称不被翻译、DIY 内容不变，以及常用主界面和电源控制界面无文字截断。用户确认 L2 后，才可开始 M3.5-A。
